export const BRAND_NAME = "Urban Sneakers";

export enum ROUTE {
  HOME = "/",
  ACCOUNT = "/accounts",
  PRODUCTS = "/products",
  ORDERS = "/orders",
}

export enum COOKIE {
  CART_ID = "cart_id"
}

export const PRODUCTS_PAGE_LENGTH = 10
